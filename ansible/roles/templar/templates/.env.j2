{#
  .env file generated by Ansible.
  Users can override these values via inventory or extra vars.
#}

{% for key, value in env_vars.items() %}
{{ key }}={{ value | quote }}
{% endfor %}

# Instance-specific overrides
CUDA_DEVICE={{ device | quote }}
WALLET_HOTKEY={{ wallet_hotkey | quote }}
