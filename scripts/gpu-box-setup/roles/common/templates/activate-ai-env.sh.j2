#!/bin/bash

# AI environment activation script
# This file was auto-generated by Ansible

# Add the AI virtual environment to PATH
export AI_ENV_PATH="{{ python_venv_path }}"
export PATH="$AI_ENV_PATH/bin:$PATH"

# Function to activate the AI environment
activate_ai_env() {
    echo "Activating AI Python environment (PyTorch {{ pytorch_version }} with CUDA {{ pytorch_cuda_version }})..."
    source "{{ python_venv_path }}/bin/activate"
}

# Create an alias for quick activation
alias ai-env='activate_ai_env'

echo "AI environment available. Type 'ai-env' to activate."